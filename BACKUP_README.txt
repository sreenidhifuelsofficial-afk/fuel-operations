FuelOps Enterprise Backup

This project uses CLEAN SOURCE backups.
Dependencies and secrets are excluded.

Excluded:

* node_modules/
* build/
* dist/
* .cache/
* coverage/
* .env

Restore:

1. npm install
2. cd frontend && npm install
3. cd backend && npm install
4. Start services normally.
